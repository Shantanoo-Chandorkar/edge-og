/**
 * Shared className for section labels used across playground panel components.
 * Exported as a constant so all three components (TemplateSelector, ApiUrlBuilder,
 * LivePreview) stay visually consistent without repeating the string.
 */
export const PANEL_LABEL_CLASS = 'text-xs font-medium text-slate-400 uppercase tracking-wider';
